info docker
------------
name = jeskaimage
mysql_root_password = jeska123



-----------------------

mysql -uroot -p

docker run --name jeska -p 3307:3307 -e MYSQL_ROOT_PASSWORD=jeska123 -d mysql

-----------------------



------- esto conecta a la bd de mysql
 mysql -h jtb9ia3h1pgevwb1.cbetxkdyhwsb.us-east-1.rds.amazonaws.com -u y48p0dvki4no2cot -p -D nsn4zf2b3s84x33z
------



ALTER TABLE products
ADD COLUMN ruta_imagen_principal VARCHAR(255);
ALTER TABLE products
ADD COLUMN ruta_imagen_2 VARCHAR(255);
ALTER TABLE products
ADD COLUMN ruta_imagen_3 VARCHAR(255);




CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    description text,
    price decimal(10,2) NOT NULL,
    category_id int,
    ruta_imagen_princial varchar(255),
    ruta_imagen_secuendario varchar(255),
    ruta_imagen_terciaria varchar(255), 
    FOREIGN KEY (category_id) REFERENCES category(id)
);
CREATE TABLE category (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title varchar(100) NOT NULL,
    description text
);

CREATE TABLE user (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    cedula text,
    telefono varchar(20),
    marca varchar(255),
    fabricante varchar(100),
    activo TINYINT(1),

    correo varchar(100),
    contraseña varchar(100)
     
);




insert into category (title, description) values ("bolsos", "bolsos de toda clase");
insert into category (title, description) values ("faldas", "faldas de toda clase");

insert into category (title, description) values ("camisas", "camisas de toda clase");
insert into category (title, description) values ("sandalias", "sandalias de toda clase");


insert into products (title, description, price, category_id) values(
    "bolso de amarre negro", "este alkslakjlsakjlsk", "25.000", "1"
);

insert into products (title, description, price, category_id) values(
    "bolso de amarre azul0", "este alkslakjlsakjlsk", "26.000", "2"
);

insert into products (title, description, price, category_id) values(
    "camisa negro de velo", "este alkslakjlsakjlsk", "28.000", "3"
);
insert into products (title, description, price, category_id) values(
    "sanadlia azual", "este alkslakjlsakjlsk", "28.000", "4"
);


insert into user (name, cedula, telefono, marca, fabricante, activo, correo, contrasea) values(
    "jesica", "12345678", "3141689713", "sandbox", "si", "1", "jeska@gmail.com", "jeska123"
);









